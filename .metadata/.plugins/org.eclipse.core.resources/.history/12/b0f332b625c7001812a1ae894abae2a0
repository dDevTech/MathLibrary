package utils.equations;

import java.util.ArrayList;

import utils.matrix.Matrix;

public class Solver {
		public void solver(ArrayList<EquationData>equations) {
			//A-1*B
			double [][]coefficients;
			double [][]constants;
			for(int i=0;i<equations.size();i++) {
				for(int j=0;j<equations.get(i).getIncognites();j++) {
					coefficients[i][j]=equations.get(i).getCoefficients()[j];
				}
			}
			Matrix matrix = new Matrix(values);
			Matrix matrix2 = new Matrix(values);
		
		}

}
